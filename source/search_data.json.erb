{
  <% dato.paquetes.each do |paquete| %>
    "<%= paquete.slug %>": {
      "title": "<%= paquete.titulo %>"
    }
  <% end %>
  <% dato.circuitos.each do |circuito| %>
    "<%= circuito.slug %>": {
      "title": "<%= circuito.titulo %>"
    }<% unless circuito == dato.circuito.last %>, <% end %>
  <% end %>
}
